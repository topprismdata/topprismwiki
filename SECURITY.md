# Security policy

Do not report credentials, private chat, or business documents in a public
issue. Remove the material locally, rotate exposed credentials, and contact the
repository maintainer through a private channel before opening a disclosure.

The runner is designed to keep raw evidence in a private Workspace and to make
formal Vault writes transactional. This is a workflow safeguard, not a
guarantee that an incorrectly configured model endpoint is private.
